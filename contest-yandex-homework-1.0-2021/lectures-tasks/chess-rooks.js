// задача про ладьи

function countBeatingRooks(rookCoords) {
    function addRook(roworCol, key) {

    }

    function countPairs(roworCol) {

    }

    const rooksInRow = {};
    const rooksInCol = {};

    for (let i = 0; i < rookCoords.length; i++) {
        addRook(rooksInRow, i[0])
        addRook(rooksInCol, i[1])
    }
}